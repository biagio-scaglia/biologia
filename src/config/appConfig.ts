import { AppConfig } from '@/types';

/**
 * Configurazione principale dell'applicazione
 */
export const appConfig: AppConfig = {
  appName: 'Istiocitosi a Cellule di Langerhans',
  version: '1.0.0',
  defaultTheme: 'light',
  apiEnabled: false,
};

